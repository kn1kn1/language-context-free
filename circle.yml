dependencies:
  pre:
      - wget -O atom-amd64.deb https://atom.io/download/deb?channel=beta
      - sudo dpkg --install atom-amd64.deb || true
      - sudo apt-get -f install
      - apm-beta install
test:
  override:
    - apm-beta test -p /usr/bin/atom-beta
