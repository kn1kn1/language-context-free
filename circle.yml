dependencies:
  override:
    - curl -s -O https://raw.githubusercontent.com/kn1kn1/ci/feature/atom-beta-circleci/build-package.sh
    - chmod u+x build-package.sh

test:
  override:
    - ./build-package.sh

machine:
  environment:
    ATOM_LINT_WITH_BUNDLED_NODE: "true"
    APM_TEST_PACKAGES: ""
    ATOM_CHANNEL: "beta"
