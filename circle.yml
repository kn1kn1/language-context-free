dependencies:
  pre:
    - curl -L https://atom.io/download/deb?channel=beta -o atom-amd64.deb
    - sudo dpkg --install atom-amd64.deb || true
    - sudo apt-get -f install
    - apm-beta install
test:
  override:
    - apm-beta test -p /usr/bin/atom-beta
